plane[0];
plane.length;
plane.indexOf(i);
plane.lastIndexOf(i);
plane.slice(2);
plane.slice(0, 4);

// negative slice

plane.slice(-1);
plane.slice(1, -1);

// practicle example using slice and index of method

plane.slice(0, plane.indexOf(" "));
plane.slice(plane.lastIndexOf(" "));

// part2

toLowerCase();
toUpperCase();
slice();
trim();
replace();
startsWith();
endsWith();
includes();

// part 3
